# 仪表盘排障指南

## 快速自检清单

按顺序检查，90%的问题在前两步就能定位。

### 1. 页面打不开（404/500）

| 检查项 | 怎么做 |
|--------|--------|
| URL 是否正确 | `https://eternalee87.github.io/A-SHARE-dashboard/`（注意大小写 A-SHARE） |
| GitHub Pages 是否启用 | 打开 https://github.com/eternalee87/A-SHARE-dashboard/settings/pages → 确认有绿色提示 |
| 仓库是否存在 | https://github.com/eternalee87/A-SHARE-dashboard |

### 2. 数据没更新（还是昨天的）

| 检查项 | 怎么做 |
|--------|--------|
| Actions 最近一次运行 | https://github.com/eternalee87/A-SHARE-dashboard/actions → 看最新的 `Hourly Dashboard Update` |
| 运行失败则看日志 | 点进失败的运行 → 展开 `Fetch latest data` 步骤 → 看报错信息 |
| 手动触发一次 | Actions 页 → `Run workflow` → `Run workflow` |

**常见失败原因：**
- akshare API 变动 → 更新 akshare 版本：`pip install akshare --upgrade`
- 腾讯 API 限流 → 等一小时自动重试
- 某个指数代码失效 → 查看日志中哪个 symbol 报错

### 3. 页面显示但样式错乱/图表不显示

| 检查项 | 怎么做 |
|--------|--------|
| 浏览器控制台 | F12 → Console → 看红色报错 |
| 清除缓存 | Ctrl+Shift+R（Windows）/ Cmd+Shift+R（Mac）强制刷新 |
| Chart.js CDN | 确认能访问 `https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js` |

### 4. 本地诊断命令

```powershell
cd "C:\Users\zhouy\AppData\Roaming\reasonix\Investment Analysis"

# 检查数据新鲜度
python -c "import pandas as pd; df=pd.read_csv('data/style_indices_v2.csv',index_col=0,parse_dates=True); print('最新:', df.index[-1])"

# 手动跑一次完整流程
python fetch_data.py
python gen_data.py
python build_html.py

# 本地预览
python server.py
```

### 5. 彻底重建

```powershell
# 如果一切都不对，从远程重新拉取
git fetch origin
git reset --hard origin/main
python fetch_data.py
python gen_data.py
python build_html.py
# 浏览器打开 dashboard.html
```

---

## 联系我（Reasonix）

打开 Reasonix，新建对话，把以下信息粘贴给我：

```
项目路径：C:\Users\zhouy\AppData\Roaming\reasonix\Investment Analysis
问题描述：[页面打不开 / 数据停更 / 图表异常 / 其他]
Actions 日志链接（如有）：https://github.com/eternalee87/A-SHARE-dashboard/actions/runs/...
```

如果 Actions 日志太长，可以把关键报错截图发给我。
